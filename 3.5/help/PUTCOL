PRINT ' '
PRINT 'PUTCOL:  (VERB) Places the contents of Array (0) into'
PRINT 'column n of Matrix (0).  If Array (0) is longer than the'
PRINT 'length of a column in Matrix (0), the extra values are'
PRINT 'ignored.  If Array (0) is shorter than the length of a'
PRINT 'column in Matrix (0), the column is padded with blank'
PRINT 'values.'
PRINT ''
PRINT 'Syntax: PUTCOL(n)'
PRINT ' '
