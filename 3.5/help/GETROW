PRINT ' '
PRINT 'GETROW:  (VERB) Places the values from row n of Matrix (0)'
PRINT 'into Array (0).  If Array (0) can hold more values than'
PRINT 'a row of Matrix (0), blank values are added to the extra'
PRINT 'Array (0) locations.  If Array (0) can hold fewer values'
PRINT 'than a row of Matrix (0), the extra values are ignored.'
PRINT ' '
PRINT 'Syntax: GETROW(n)'
PRINT ' '
