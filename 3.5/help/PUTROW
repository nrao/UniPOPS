PRINT ' '
PRINT 'PUTROW:  (VERB) Places the contents of Array (0) into'
PRINT 'row n of Matrix (0).  If Array (0) is longer than the'
PRINT 'length of a row in Matrix (0), the extra values are'
PRINT 'ignored.  If Array (0) is shorter than the length of a'
PRINT 'row in Matrix (0), the row is padded with blank'
PRINT 'values.'
PRINT ''
PRINT 'Syntax: PUTROW(n)'
PRINT ' '
