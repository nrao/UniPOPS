PRINT ' '
PRINT 'DEFMODE:  (ADVERB) Used by VERBs which work with two'
PRINT 'arrays to indicated how blanked values should be treated.'
PRINT ''
PRINT 'If DEFMODE is FALSE, the resulting array will have blank'
PRINT 'values where there are blank values in EITHER of the'
PRINT 'original arrays.'
PRINT ''
PRINT 'If DEFMODE is TRUE, the resulting array will have blank'
PRINT 'values where there are blank values in BOTH of the original'
PRINT 'arrays.  If data is blanked in one array but not blanked in'
PRINT 'the other, the verb will use the non-blanked value in its'
PRINT 'calculation.'
PRINT ' '
