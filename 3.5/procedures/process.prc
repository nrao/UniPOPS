procedure process
#  place processing steps here, things to do to a scan before
#  saving it (used with the prcstk procedure).
#  Suppose you want to remove a 0th order baseline (dc offset) from each
#  scan.  This is how you might do that.
#  (the baseline fitting ranges, nregion, should be set first)
nfit = 0; baseline
return
finish
